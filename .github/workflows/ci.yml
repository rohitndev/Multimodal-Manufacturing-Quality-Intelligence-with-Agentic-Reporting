name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install minimal test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install numpy pillow opencv-python-headless pydantic fastapi httpx pytest pytest-asyncio python-multipart requests
      - name: Run unit tests
        run: pytest tests -q --disable-pytest-warnings
